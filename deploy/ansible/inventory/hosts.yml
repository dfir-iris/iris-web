---
all:
  children:
    control_nodes:
      hosts:
        control-node:
          ansible_host: localhost
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3

    iris_servers:
      hosts:
        # managed-node:
        #   ansible_host: localhost
        #   ansible_port: 2222
        #   ansible_user: ansible
        #   ansible_ssh_pass: ansible123
        #   ansible_python_interpreter: /usr/bin/python3

        # # Example: Ubuntu Multipass node configuration
        # ubuntu-multipass:
        #   ansible_host: 192.168.64.100
        #   ansible_user: ubuntu
        #   ansible_ssh_private_key_file: ~/.ssh/id_rsa
        #   ansible_python_interpreter: /usr/bin/python3
        iris-server-example:
          ansible_host: 192.168.1.100
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
          ansible_python_interpreter: /usr/bin/python3
      vars:
        # Common variables for all IRIS servers
        iris_project_name: "iris-web"
        iris_deployment_mode: "development"
